framework:
  github: supranim/supranim
  version: 0.1
  build_opts:
    - "--mm:arc"
    - "-d:webapp"
    - "-d:httpbench"

server:
  command: "/usr/src/app/server start . --port:3000"

build_deps:
  - libpcre3-dev
  - libevent-dev